import React from 'react';
import {Image} from 'react-native';
import {ICONS} from '../../constants/icons';

export const ArrowDownBlack = () => {
  return (
    <Image source={ICONS.arrowDownBlack} style={{height: 24, width: 24}} />
  );
};
