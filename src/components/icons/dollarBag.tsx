import React from 'react';
import {Image} from 'react-native';
import {ICONS} from '../../constants/icons';

export const DollarBag = () => {
  return <Image source={ICONS.dollarBag} style={{height: 32, width: 26.67}} />;
};
