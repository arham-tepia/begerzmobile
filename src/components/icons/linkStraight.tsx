import React from 'react';
import {Image} from 'react-native';
import {ICONS} from '../../constants/icons';

export const LinkStraight = () => {
  return <Image source={ICONS.linkStraight} style={{height: 24, width: 24}} />;
};
