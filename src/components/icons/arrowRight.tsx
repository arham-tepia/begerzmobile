import React from 'react';
import {Image} from 'react-native';
import {ICONS} from '../../constants/icons';

export const ArrowRight = () => {
  return (
    <Image source={ICONS.arrowRight} style={{height: 9.04, width: 4.54}} />
  );
};
