import React from 'react';
import {Image} from 'react-native';
import {ICONS} from '../../constants/icons';

export const ArrowDownFilled = () => {
  return (
    <Image source={ICONS.arrowDownFilled} style={{height: 24, width: 24}} />
  );
};
