import React from 'react';
import {Image} from 'react-native';
import {ICONS} from '../../constants/icons';

export const StarColored = () => {
  return <Image source={ICONS.starColored} style={{height: 50, width: 50}} />;
};
